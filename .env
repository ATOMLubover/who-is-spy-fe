# Move environment-specific variables to .env.development / .env.production
# This file should not contain development-only endpoints that leak into production builds.

# Example: set common flags here, but keep service URLs in mode-specific files.